// globals.js
// Stellt globale Variablen für die App bereit

// Manuell setzen: true = lokal (localhost), false = IP verwenden
window.IS_LOCALHOST = true;
window.LOCAL_IP = window.IS_LOCALHOST ? "localhost" : "10.35.3.54";

